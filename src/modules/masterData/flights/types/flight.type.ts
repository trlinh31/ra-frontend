export type Flight = {
  id: string;
  code: string;
  airlineCode: string;
  origin: string;
  destination: string;
  airline: string;
  flightTime: string;
  price: number;
  notes: string;
  isActive: boolean;
};
